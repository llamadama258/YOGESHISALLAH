python-dotenv==1.0.1
openai>=1.3.0
anthropic>=0.7.0
fastapi>=0.104.0
uvicorn>=0.24.0
transformers>=4.35.0
torch>=2.9.0
# llama-cpp-python only needed for local GGUF models - requires C++ build tools
# pip install llama-cpp-python if you want to use local models later
